# MoneyTree

MoneyTree is a local-first AI image generation workspace built with Next.js.

It includes:
- module-based reference image setup
- a Studio editor for image refinement
- a persistent gallery for generated results

## Run

```bash
npm install
npm run dev
```

Open [http://localhost:3001](http://localhost:3001).
